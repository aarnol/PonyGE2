# Prepping the data
Use the load_test.ipynb (changing the feature extractor copied from the chosen LLM) to generate a train.csv file. Move this to datasets/eeg. Then, run src/pipeline.py to run grammatical  evolution and get the corresponding output in /results/ponyge_output.txt
